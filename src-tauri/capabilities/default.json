{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permission set for the TaktDock widget window",
  "windows": ["widget", "accent"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:event:default",
    "core:window:allow-close"
  ]
}
